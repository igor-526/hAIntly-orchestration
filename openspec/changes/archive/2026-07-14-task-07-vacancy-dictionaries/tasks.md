## 1. Backend

### vacancy-service

- [x] 1.1 Добавить и зафиксировать зависимости aiohttp/Celery/Redis и валидируемые настройки HH API, `HH-User-Agent`, timeout/retry, Celery broker/backend/lock и положительного максимального возраста справочников без production hardcode.
- [x] 1.2 Спроектировать ORM-модели нормализованных справочников и реестра синхронизации с HH ID, иерархиями, `active`, timestamps и уникальными ограничениями для каждого доменного пространства.
- [x] 1.3 Создать и проверить Alembic-миграцию таблиц, внешних ключей, уникальных ограничений и индексов для пагинации и регистронезависимого поиска.
- [x] 1.4 Реализовать типизированный HH-адаптер для согласованных GET-операций с обязательным user-agent, валидацией payload, timeout и bounded retry временных ошибок.
- [x] 1.5 Реализовать репозитории транзакционного upsert полного снимка, обновления полей, повторной активации и деактивации отсутствующих элементов без JSON-дампов и дубликатов.
- [x] 1.6 Реализовать реестр попыток/успехов и защиту последнего успешного состояния от пустого, неполного или невалидного снимка.
- [x] 1.7 Настроить Celery app, Redis-lock на логический справочник и независимые идемпотентные задачи синхронизации семи семейств справочников.
- [x] 1.8 Реализовать ежечасную Beat-проверку, которая ставит задачи только для отсутствующих, никогда не синхронизированных или просроченных справочников.
- [x] 1.9 Добавить структурированные безопасные логи задач с именем справочника, результатом, длительностью и агрегированными счётчиками без секретов и полных HH payload.
- [x] 1.10 Реализовать внутренние read-only LIST/GET endpoints всех семейств с `X-User-Id`, active-only поведением, пагинацией, родительскими фильтрами и ограниченным регистронезависимым буквальным поиском.
- [x] 1.11 Добавить модульные тесты настроек, HH-адаптера, порога актуальности, Redis-lock, разворачивания иерархий, upsert/reactivate/deactivate и отказа при невалидном снимке.
- [x] 1.12 Добавить интеграционные тесты миграций, уникальности/связей, независимых транзакций и LIST/GET API, включая `404`, `X-User-Id`, пагинацию, escaping и предел `q`.
- [x] 1.13 Обновить `.env.example`, сервисный Makefile и документацию `vacancy-service`, не помещая credentials или рабочие URL в tracked-файлы.
- [x] 1.14 Обновить `docker-compose.vacancy.yml` отдельными worker/beat на общем инфраструктурном Redis без PostgreSQL/Redis сервиса и сохранить project name `haintly-vacancy-service` и порт приложения `8103`.
- [x] 1.15 Обновить корневой Makefile целями `vacancy` и `vacancy-build`, где build только собирает образы и не запускает контейнеры.

### main-be

- [x] 1.16 Добавить валидируемые настройки HTTP-зависимости `vacancy-service` и клиент-адаптер с timeout, безопасным преобразованием ошибок и передачей UUID аутентифицированного пользователя в `X-User-Id`.
- [x] 1.17 Реализовать авторизованные read-only proxy LIST/GET endpoints справочников без собственного хранения данных и без write-операций.
- [x] 1.18 Добавить модульные и интеграционные тесты proxy для cookie-auth, параметров поиска/пагинации, `401`, `404`, ошибок валидации, timeout и недоступности зависимости.
- [x] 1.19 Обновить безопасный env-пример и документацию `main-be` для новой зависимости без hardcoded production-адреса или service credential.

### Runtime

- [x] 1.20 Выполнить `lint`, `format` и `test` полного применимого пайплайна `vacancy-service` и `main-be`, устранив найденные ошибки.
- [x] 1.21 Выполнить `vacancy-build` и применимую сборку `main-be` без запуска, затем поднять инфраструктуру и изменённые контейнеры с пересборкой.
- [x] 1.22 Дождаться успешных миграций и healthcheck `vacancy-service`/`main-be`, проверить статусы worker/beat и изучить логи всех связанных контейнеров на ошибки и утечки секретов.
- [x] 1.23 Выполнить direct smoke LIST/GET к `vacancy-service` с валидным `X-User-Id` и proxy smoke через `main-be` с авторизованной cookie, включая поиск, пагинацию и отрицательные ответы.
- [x] 1.24 Выполнить e2e фоновой обработки с реальными app/worker/beat/Redis/PostgreSQL: проверить полную синхронизацию при пустой БД, только отсутствующие справочники, только просроченные справочники и отсутствие задач при актуальном состоянии, подтвердив записи/активность в БД и безопасные логи.
- [x] 1.25 Заменить поверхностную проверку HH payload на типизированную валидацию согласованных ответов `/dictionaries`, `/areas`, `/areas/countries`, `/professional_roles`, `/industries`, `/metro`, `/metro/{city_id}` и `/languages`, чтобы неполный вложенный снимок откатывался без деактивации действующих записей.
- [x] 1.26 Ограничить proxy `main-be` явным allowlist согласованных LIST/GET маршрутов справочников и покрыть маршрутизацию, cookie-auth, передачу параметров и `X-User-Id`, `401`, `404`, `422`, timeout, недоступность и некорректный ответ зависимости интеграционными тестами.
- [x] 1.27 Добавить отсутствующие тесты `vacancy-service`: timeout/bounded retry и заголовок HH-клиента, Redis-lock и конкурентную доставку, due threshold, транзакционные upsert/update/deactivate/reactivate и откат невалидного снимка, миграцию/FK/unique/index, active-only LIST/GET, UUID header, пагинацию, родительские фильтры, literal escaping и предел `q`.
- [x] 1.28 Сделать Redis-lock ownership-safe при истечении TTL и освобождении, а структурированные счётчики синхронизации — полными для всех связанных таблиц категорий/ролей и городов/линий/станций метро; покрыть оба поведения тестами.
- [x] 1.29 Исправить container runtime worker/beat и миграций так, чтобы они использовали locked production environment без скачивания dev-зависимостей при старте, запускались непривилегированным пользователем и не сохраняли `celerybeat-schedule*` в bind-mounted исходниках.
- [x] 1.30 Повторить и автоматизировать воспроизводимый e2e на реальных PostgreSQL/Redis/app/worker/beat для пустого, частично отсутствующего, просроченного и полностью актуального реестра, а также отдельно доказать upsert/deactivate/reactivate, сохранение последнего успешного снимка при ошибке и отсутствие лишних Celery-задач.
- [x] 1.31 Реально добавить заявленные задачами 1.26–1.27 автоматические проверки: HH header/timeout/bounded retry; Redis-lock/concurrency; due threshold; repository upsert/update/deactivate/reactivate/rollback; Alembic/FK/unique/index; direct LIST/GET active-only, UUID, `404`, pagination, filters, literal escaping и предел `q`; HTTP proxy с cookie-auth, передачей query/`X-User-Id`, `401`, `404`, `422`, timeout, недоступным сервисом и невалидным ответом.
- [x] 1.32 Переработать `scripts/check-vacancy-dictionaries-runtime.sh` в детерминированный e2e: изолировать/очищать тестовое состояние, покрыть первичную синхронизацию пустой БД и четыре состояния актуальности, дожидаться terminal Celery results, проверять реляционные записи и реестр, upsert/deactivate/reactivate, сохранение последнего успешного снимка при ошибке и точное отсутствие лишних задач; исключить гонку между enqueue и ручным изменением реестра.
- [x] 1.33 Сделать стандартный `make test` `vacancy-service` воспроизводимым для live PostgreSQL/Redis-проверок из документированного окружения: явно поднимать/проверять зависимости либо передавать host-доступные test URLs и credentials, не используя недоступные с хоста Docker DNS-имена; добиться прохождения всех 20 тестов одной командой.
- [x] 1.34 Усилить автоматические API/e2e-доказательства: добавить FastAPI proxy-тесты с реальной cookie-auth dependency и ответами `401/404/422`, проверять содержимое active-only/pagination/parent/literal direct API вместо одного status code, а runtime-script должен действительно очищать реляционные таблицы перед первичной синхронизацией и проверять точное множество всех семи initial задач и восстановленные записи.

## 2. Quality Gate

- [x] 2.1 Отдельным Quality Gate Agent проверить diff против proposal/design/delta specs, границы владения данными, HH OpenAPI-контракты, read-only API, конфигурацию, безопасность, отсутствие секретов и актуальность `tasks.md`.
- [x] 2.2 Повторно выполнить применимые lint/format/test и сборку изменённых образов без запуска, зафиксировав результаты по `vacancy-service` и `main-be`.
- [x] 2.3 Поднять пересобранные контейнеры, проверить миграции, healthchecks, worker/beat и логи связанных контейнеров.
- [x] 2.4 Повторить direct smoke `vacancy-service` и авторизованный proxy smoke через `main-be`, включая положительные и отрицательные LIST/GET сценарии.
- [x] 2.5 Повторить e2e четырёх состояний актуальности и проверить конечные реляционные записи, upsert/deactivate/reactivate, реестр синхронизации и отсутствие лишних задач.
- [x] 2.6 При статусе `НА ДОРАБОТКУ` превратить замечания в атомарные задачи соответствующих секций, передать реализацию профильному агенту и повторять Quality Gate до `ОДОБРЕНО`.
- [x] 2.7 После `ОДОБРЕНО` создать отчёт `docs/reports/task-07-vacancy-dictionaries.md` по `docs/reports/TEMPLATE.md` с выполненными проверками, рисками и итоговым статусом.
