## MODIFIED Requirements

### Requirement: Шапка управляет HH-аккаунтами
На защищённой главной странице `main-fe` MUST показывать на всю ширину текстовый логотип «HAIntly», меню профиля и trigger активного HH-аккаунта; trigger MUST открывать доступный MUI `Drawer` сверху со списком всех связей и действиями добавления, выбора и удаления любого аккаунта, а удаление MUST требовать подтверждения в MUI modal dialog.

#### Scenario: Пользователь имеет несколько аккаунтов
- **WHEN** загружены две или более принадлежащие пользователю связи и пользователь открывает trigger
- **THEN** сверху открывается Drawer, который показывает все связи с явным active state и позволяет выбрать другую связь без возможности явно снять выбор

#### Scenario: Закрытие Drawer
- **WHEN** пользователь нажимает Escape, активирует доступное действие закрытия или взаимодействует с backdrop вне pending-операции
- **THEN** Drawer закрывается, а фокус возвращается на trigger управления HH-аккаунтами

#### Scenario: Добавление аккаунта
- **WHEN** пользователь выбирает в Drawer действие добавления
- **THEN** запускается существующий popup-поток HH OAuth, а после успеха список и активный выбор обновляются

#### Scenario: Выбор другого аккаунта
- **WHEN** пользователь выбирает неактивную связь в Drawer
- **THEN** UI вызывает существующий proxy `main-be`, обновляет active state и отражает выбранный аккаунт в trigger

#### Scenario: Удаление любого аккаунта
- **WHEN** пользователь выбирает удаление активной или неактивной связи в Drawer
- **THEN** UI не отправляет DELETE-запрос и открывает доступный modal dialog с именем именно выбранного профиля и явными действиями «Отмена» и «Удалить»

#### Scenario: Подтверждённое удаление аккаунта
- **WHEN** пользователь подтверждает удаление выбранного принадлежащего аккаунта
- **THEN** UI вызывает существующий proxy `main-be` для id выбранной связи, обновляет список и отражает прежний, новый активный либо пустой выбор согласно ответу backend

#### Scenario: Безопасное состояние удаления
- **WHEN** удаление выполняется или завершается ошибкой
- **THEN** pending исключает повторную отправку, Escape и отмена не прерывают выполняющийся запрос, а при ошибке dialog сохраняет правильную цель и позволяет безопасно повторить либо отменить операцию

#### Scenario: Меню профиля
- **WHEN** пользователь открывает меню профиля
- **THEN** интерфейс показывает неактивный пункт «Настройки» и активный пункт «Выход», который использует существующий auth logout flow
