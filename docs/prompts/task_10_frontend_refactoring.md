# Контекст
Есть проблема с тем, что `main-fe` не билдится и там структура не соответствует той, что должна быть
Мы работаем не с разработкой, а с рефакторингом и настройкой агента. Не используй маршрутизацию, оркестратор и OpenSpec. Доверяй задачи субагентам как посчитаешь нужным в обход правилам
# Issue 1. Build error
При выполнении команды `make fe-build` процесс падает с ошибкой. Необходимо исправить
# Issue 2. Linting and testing
Необходимо задать более строгие правила для линтинга
# Issue 3. Architecture
Архитектура frontend приложения не сходится с той, что должна быть
Посмотри архитектуру и агентов следующих проектов:
1. `/home/igor/projects/nexora-seo/orchestration` (services/fe)
2. `/home/igor/projects/eqSiteCMS` (services/frontend) ЭТАЛОН
Мне нужна папка src в корне, которая содержит следующую структуру:
api - всё взаимодействие с API бэкэнда
app - только роутинг. Страницы состоят из единого элемента 
contexts - контексты
features - каждая фича сайта в отдельной папке. Каждая фича разделена на hooks, services, ui и validators. В ui хранится компонент-страница (которая потом вызывается из app). Все переиспользуемое выносится
hooks  - общие хуки, которые используются из любого места приложения
lib - функции, которые используются приложением
test 
types - все типы
ui - только те компоненты, которые переиспользуются в разных фичах

Необходим полный рефакторинг
# Issue 4. Agents
1. В протокол QualityGate для frontend помимо тестирования необходимо добавить линтинг
2. В протокол QualityGate для frontend необходимо добавить build проекта. После билда никаких артефактов оставаться не должно. Можно использовать для этого docker compose (команда `make fe-build их корня оркестратора)
3. Необходимо доработать агента frontend, корректно описав всю архитектуру, чтобы в последствии всё было корректно и на своих местах
4. Ни в коем случае не смягчаем правила линтинга. Допустимо только ужесточение
# Issue 5. Commands
Тестирование и линтинг производятся следующими командами:
- `npm test`
- `npm run lint`
- `npx tsc --noEmit`